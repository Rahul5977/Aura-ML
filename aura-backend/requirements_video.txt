# ============================================================================
# Week 8 - Video Analysis Requirements
# Scene Analysis Pipeline Dependencies
# ============================================================================

# Core ML/DL Frameworks
# ----------------------------------------------------------------------------
torch>=2.0.0                    # PyTorch for deep learning
torchvision>=0.15.0             # PyTorch vision utilities
transformers>=4.35.0            # Hugging Face Transformers (LLaVA)
accelerate>=0.24.0              # Fast model loading and inference
bitsandbytes>=0.41.0           # 8-bit quantization for memory efficiency

# Computer Vision
# ----------------------------------------------------------------------------
opencv-python>=4.8.0            # Video processing and frame extraction
opencv-python-headless>=4.8.0   # Headless OpenCV (no GUI dependencies)
Pillow>=10.0.0                  # Image processing library

# Face Analysis (Optional - for extended pipeline)
# ----------------------------------------------------------------------------
facenet-pytorch>=2.5.3          # MTCNN and InceptionResnetV1
scipy>=1.11.0                   # Scientific computing (for similarity)

# Utilities
# ----------------------------------------------------------------------------
numpy>=1.24.0,<2.0.0           # Numerical computing (compatible version)
tqdm>=4.66.0                    # Progress bars
python-dateutil>=2.8.0          # Date/time utilities

# API and Web (if using FastAPI endpoints)
# ----------------------------------------------------------------------------
fastapi>=0.104.0                # Web framework
uvicorn[standard]>=0.24.0       # ASGI server
python-multipart>=0.0.6         # File upload support

# Development and Testing
# ----------------------------------------------------------------------------
pytest>=7.4.0                   # Testing framework
pytest-asyncio>=0.21.0          # Async testing support
ipython>=8.12.0                 # Interactive Python shell
jupyter>=1.0.0                  # Jupyter notebooks
matplotlib>=3.7.0               # Plotting and visualization

# Optional: GPU Acceleration
# ----------------------------------------------------------------------------
# For CUDA 11.8 (uncomment if needed)
# torch==2.1.0+cu118
# torchvision==0.16.0+cu118
# --extra-index-url https://download.pytorch.org/whl/cu118

# For CUDA 12.1 (uncomment if needed)
# torch==2.1.0+cu121
# torchvision==0.16.0+cu121
# --extra-index-url https://download.pytorch.org/whl/cu121

# ============================================================================
# Installation Notes
# ============================================================================
# 
# Basic Installation:
#   pip install -r requirements_video.txt
#
# With GPU Support (CUDA 11.8):
#   pip install torch torchvision --index-url https://download.pytorch.org/whl/cu118
#   pip install -r requirements_video.txt
#
# Memory-Efficient Mode:
#   Install bitsandbytes for 8-bit model loading:
#   pip install bitsandbytes
#
# Troubleshooting:
#   - If numpy version conflict: pip install "numpy>=1.24.0,<2.0.0" --force-reinstall
#   - If opencv conflicts: pip install opencv-python-headless (instead of opencv-python)
#   - For M1/M2 Mac: Use conda for better compatibility with torch
# ============================================================================
